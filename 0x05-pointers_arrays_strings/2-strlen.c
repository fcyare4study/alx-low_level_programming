#include "main.h"

/**
 * _strlen - returns the length of a string.
 * @s: the string to be operated on.
 *
 * Return: int.
 */

int _strlen(char *s)
{
	int i = 0;

	while (c[i] != '\0')
		i++;
	return (i);
}
